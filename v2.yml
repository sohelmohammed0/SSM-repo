name: SSM Combined Pipeline (OIDC)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        type: choice
        options:
          - feature
          - dev
          - test
          - prod

      storage:
        description: "Storage"
        required: true
        type: choice
        options:
          - PARAMETER STORE

      parameter_key:
        description: "Parameter Key"
        required: true

      parameter_value:
        description: "Parameter Value"
        required: true

      team_name:
        description: "Team Name"
        required: true

      is_secret:
        description: "Is it a secret string?"
        required: true
        type: boolean
        default: true

      overwrite:
        description: "Should overwrite if already exists?"
        required: true
        type: boolean
        default: true

      export_env:
        description: "Should store in env variables?"
        required: true
        type: boolean
        default: false

  pull_request:
    branches:
      - dev
      - test
      - prod
      - "feature/*"

# 🔐 REQUIRED FOR OIDC
permissions:
  id-token: write
  contents: read

jobs:
  ssm-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 🔐 OIDC AUTH (NO AWS KEYS)
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::412128478995:role/TestForPMS
          aws-region: us-east-1

      # =============================
      # 🔹 MANUAL MODE (STORE SECRET)
      # =============================
      - name: Validate Inputs
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          if [ -z "${{ github.event.inputs.parameter_key }}" ] || [ -z "${{ github.event.inputs.parameter_value }}" ]; then
            echo "❌ Parameter key/value cannot be empty"
            exit 1
          fi

      - name: Build Parameter Name
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          PARAM_NAME="/${{ github.event.inputs.team_name }}/${{ github.event.inputs.environment }}/${{ github.event.inputs.parameter_key }}"
          echo "PARAM_NAME=$PARAM_NAME" >> $GITHUB_ENV

      - name: Set Parameter Type
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          if [ "${{ github.event.inputs.is_secret }}" = "true" ]; then
            echo "TYPE=SecureString" >> $GITHUB_ENV
          else
            echo "TYPE=String" >> $GITHUB_ENV
          fi

      - name: Store or Update Parameter in SSM
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          echo "Storing parameter: $PARAM_NAME"

          aws ssm put-parameter \
            --name "$PARAM_NAME" \
            --value "${{ github.event.inputs.parameter_value }}" \
            --type "$TYPE" \
            --overwrite

      - name: Export to GitHub ENV (optional)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.export_env == 'true' }}
        run: |
          echo "${{ github.event.inputs.parameter_key }}=${{ github.event.inputs.parameter_value }}" >> $GITHUB_ENV

      # =============================
      # 🔹 PR MODE (FETCH SECRETS)
      # =============================
      - name: Detect Environment from Branch
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          BRANCH=${GITHUB_HEAD_REF}

          if [[ "$BRANCH" == feature/* ]]; then
            ENV="feature"
          elif [[ "$BRANCH" == dev ]]; then
            ENV="dev"
          elif [[ "$BRANCH" == test ]]; then
            ENV="test"
          elif [[ "$BRANCH" == prod ]]; then
            ENV="prod"
          else
            echo "❌ Unknown branch: $BRANCH"
            exit 1
          fi

          echo "ENV=$ENV" >> $GITHUB_ENV

      - name: Fetch All Parameters from SSM
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          TEAM="payments"   # 🔧 change if needed
          PATH="/$TEAM/$ENV/"

          echo "Fetching parameters from: $PATH"

          aws ssm get-parameters-by-path \
            --path "$PATH" \
            --with-decryption \
            --recursive \
            --query "Parameters[*].[Name,Value]" \
            --output text | while read NAME VALUE; do

              KEY=$(basename $NAME)
              echo "$KEY=$VALUE" >> $GITHUB_ENV
            done

      - name: Use Secrets (Example)
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          echo "✅ Secrets loaded into environment"